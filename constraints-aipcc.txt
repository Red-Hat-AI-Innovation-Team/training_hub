# AIPCC 3.5 GA constraint file
#
# Use with: uv pip install -e ".[grpo,lora]" -c constraints-aipcc.txt
#
# These pins match the AIPCC 3.5 GA package index. Without them,
# the resolver picks versions that are incompatible with each other
# (e.g. old unsloth that doesn't work with transformers 5.13.x).
#
# Packages listed under "--no-deps installs" below must be installed
# SEPARATELY with --no-deps after the main install, because their
# declared dependency caps conflict with the required versions.
# See docs/guides/aipcc-install.md for the full recipe.

# Core stack
torch==2.11.0
transformers==5.13.0
kernels==0.15.2
peft==0.19.1
openpipe-art==0.5.18
accelerate==1.14.0
trl==1.6.0
numba==0.65.0
fsspec==2026.4.0
vllm==0.21.0

# Transitive dependencies pinned to AIPCC versions
cffi==2.0.0
charset-normalizer==3.4.7
cuda-python==13.0.3
fastapi-cli==0.0.28
filelock==3.29.5
grpcio==1.81.1
huggingface-hub==1.22.0
litellm==1.75.8
multiprocess==0.70.16
openai==2.44.0
polars==1.31.0
pyarrow==24.0.0
regex==2026.6.28
timm==1.0.27
tqdm==4.68.3
uvicorn==0.50.0
websockets==16.0
xxhash==3.8.0

# --no-deps installs (NOT handled by this constraints file):
#   uv pip install unsloth==2026.7.2 --no-deps
#   uv pip install unsloth_zoo==2026.7.2 --no-deps
#   uv pip install megatron-core --no-deps
#
# Why --no-deps:
#   unsloth caps transformers<=5.5.0 — conflicts with required 5.13.0
#   unsloth_zoo has the same caps
#   megatron-core is only needed for ART 0.5.18 import stubs (~2 MB)
